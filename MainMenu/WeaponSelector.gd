extends OptionButton

var weapon_select = "test"


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
#	for Weapon in weapons:
#		var weapon_name = Weapon
#		add_item(weapon_name)
	pass
